# App theme (safe to commit — no secrets here).
[theme]
base = "light"
primaryColor = "#7C4DFF"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F6F4FF"
textColor = "#1E1E2E"
font = "sans serif"
